# Asset Guide

当前 UI 使用 Win32/GDI 绘制，assets 提供课程项目可替换的占位贴图。发布时 CMake 会复制整个 `assets/` 目录。

允许使用的素材：

- 自制像素图或 PPM/BMP/PNG。
- CC0、公有领域或允许再分发的宽松许可证素材。

添加外部素材时必须更新 `assets/ATTRIBUTION.md`，记录文件名、来源、作者、许可证、原始页面和是否修改。

当前 UI 不依赖图片加载，因此素材缺失不会导致崩溃；后续若改成贴图加载，应在加载失败时回退到 GDI 色块或占位图。

